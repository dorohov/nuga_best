### В шаблоне есть 3 модалки, вызываются они так:

```javascript
$('#modal__message_on').modal() // Сообщение об успешной отправки формы
$('#modal_quest').modal()       // Форма вопроса   
$('#modal_job').modal()         // Вакансия   
```